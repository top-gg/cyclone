name: Musicium
link: https://github.com/Tomato6966/Musicium
detections:
  - name: Help command
    input: "{prefix}help"
    matcher:
      type: embed
      # prefix matchers with {?} to ignore text preceding the one being matched
      description: |
        {?}
        > Check out the [**Dashboard**]({dashboardUrl: ?}) or the [**Live Music Queue**]({queueUrl: ?})
